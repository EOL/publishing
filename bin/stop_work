#!/bin/bash
pid=`ps aux | grep jobs:work$ | awk '{print $2}'`
[[ -z "$pid" ]] && { echo "Doesn't appear to be running" ; exit 1; }
echo "Killing $pid"
kill $pid
echo "Killed $pid at `date`" >> log/harvest.log
