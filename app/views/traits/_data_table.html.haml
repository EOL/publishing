- if @search.grouped_data.empty?
  %p.uk-text-center= t("search_results.empty")
- else
  = paginate @search.grouped_data
  %ul.traits.traits-term-search
    -# TODO: helper
    - @search.grouped_data.each do |trait|
      = render :partial => "data_row", :locals => { :trait => trait, :show_taxon => true }
  = paginate @search.grouped_data
