= simple_form_for([:admin, @editor_page]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, input_html: { class: "uk-input" }
    = f.hidden_field :editor_page_directory_id

  .form-actions
    = f.button :submit, input_html: { class: "uk-submit" }
